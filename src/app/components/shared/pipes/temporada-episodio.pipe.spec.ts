import { TemporadaEpisodioPipe } from './temporada-episodio.pipe';

describe('TemporadaEpisodioPipe', () => {
  it('create an instance', () => {
    const pipe = new TemporadaEpisodioPipe();
    expect(pipe).toBeTruthy();
  });
});
