import { IdUrlDimensionPipe } from './id-url-dimension.pipe';

describe('IdUrlDimensionPipe', () => {
  it('create an instance', () => {
    const pipe = new IdUrlDimensionPipe();
    expect(pipe).toBeTruthy();
  });
});
